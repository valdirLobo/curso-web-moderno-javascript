const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
console.log(soma)

const subtracao = d - b
console.log(subtracao)

const multiplicacao = a * b
const divisao = d/a
const modulo = a % 2 // o resto da divisao que será armazenado a módulo

console.log(modulo)

console.log (multiplicacao, divisao)