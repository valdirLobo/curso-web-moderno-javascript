const resultado = nota => nota >= 7 ? "aprovado" : "reprovado"

console.log(resultado(7.1))
console.log(resultado(6.7))

//operador ternário é composto por 3 partes--> 1: expressão que vai retornar V ou F, 2:?, 3: o que retorna se for vrdadeiro : o que retorna se for falso
